# Drafted doc

> **This document owns:** a drafted doc whose sections plant one fill-gate defect each. *(draft, review me)*

<!-- concern: testing; fill: tests, ci, packages -->

## A clean section

The app declares one dependency group in its manifest [package.json]. Tests live beside the
code they cover [tests].

*(draft, review me)*

## A section still a skeleton

*Which test runners are configured and where tests live. From config files and folder names.*

## Missing brackets

This sentence states something about the repository and names no file at all.

*(draft, review me)*

## A bracket that does not resolve

The worker reads its queue name from the environment [src/workers/does-not-exist.js].

*(draft, review me)*

## A line-number citation

The rate limit is applied in the request handler [src/config.js:12].

*(draft, review me)*

## Banned wording

The export path is robust and the handler should retry, because the queue is slow [package.json].

*(draft, review me)*

## An env value

Set API_TOKEN=plaintext-value-that-must-not-be-echoed in the local file [.env.example].

*(draft, review me)*

## Shapes the gate accepts

The service list has one entry [inventory: services[0]].

*(draft, review me)*

## A file-routed citation

One route file answers the item lookup [src/app/api/items/[sku]/route.js].

*(draft, review me)*

## A cross-doc link and a quotation

The deploy steps live in [DEPLOY.md](DEPLOY.md), which owns them [DEPLOY.md]. The README says
"the exporter must not run without a token", and the name is read at start-up [.env.example].

*(draft, review me)*

## A backticked name

The helper is `src/utils/fast-path.js` and the export list names it once [package.json: name].

*(draft, review me)*

## A count nothing reports

The exporter writes 4096 files and 77 tables on every run [package.json: name].

*(draft, review me)*

## A negative with no scope

No authorization guard is applied to the export route [src/server.js].

*(draft, review me)*

## A negative that names its scope

No rate-limit code was found under `src/routes` (grep "rate") [src/server.js].

*(draft, review me)*
