# Setup

Install the app, then read the [configuration](#configuration) section below.

## Install

Clone the repository and install dependencies. The export cap defaults to 42.5% of the
row budget; see [limits](limits.md#the-cap) for why.

## Config

The token is read in `src/config.js:12` and nowhere else.
