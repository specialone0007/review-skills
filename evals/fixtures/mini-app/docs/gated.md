# Gated

> **This document owns:** the shapes the gate has to see. *(draft, review me)*

Must be installed before the suite runs [package.json]. The lead paragraph is the most-read
part of a document and was not gated at all, so a capitalised modal here is the case this
file exists to hold.

*(draft, review me)*

## A table whose middle cell cites nothing real

| command | handler | what it does |
| --- | --- | --- |
| `start` | [src/does-not-exist.js] | runs the server [src/server.js] |

*(draft, review me)*
