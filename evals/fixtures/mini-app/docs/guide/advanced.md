# Advanced export options

> **This document owns:** the flags an export accepts beyond the defaults.

This file sits in `docs/guide/`, and `docs/guide.md` is a chapter beside it, not its index:
it links nothing in this folder. The checker must fall through to the central index rather
than crediting `docs/guide.md` with indexing a folder it never mentions.
