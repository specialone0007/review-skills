# CRLF sample

> **This document owns:** nothing real; it exists with Windows line endings so anchor slugs are computed without a trailing carriage return.

## Notes

The cap is 42.5% here too, a fourth file carrying the same number.
