# Deploying the exporter

> **This document owns:** the deploy steps and the one volume that holds exported files.

The README links this file, so a reader reaches it in one hop. It sits beside the README
rather than under `docs/`, which is a layout the checker has to accept rather than answer
by laying a thinner skeleton down next to it.

## Steps

1. Build the image.
2. Attach the volume.
3. Start the service.
