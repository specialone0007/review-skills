{
  "name": "@mini/exporter",
  "version": "0.0.0",
  "private": true,
  "license": "Apache-2.0",
  "engines": { "node": ">=20" },
  "type": "module",
  "scripts": {
    "dev:start": "node src/server.js",
    "build": "echo no build step",
    "test": "vitest run",
    "postinstall": "node -e \"console.log('fixture postinstall, does nothing')\""
  },
  "dependencies": {
    "left-pad": "*",
    "local-helper": "file:../local-helper"
  },
  "devDependencies": {
    "vitest": "^2.0.0"
  }
}
