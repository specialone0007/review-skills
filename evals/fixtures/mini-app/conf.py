# A config file that shares a docs-generator filename without being one. The checker must not
# switch R1, R4, R11 and R12 off for the whole repo on the strength of a filename.
DEBUG = True
RETRIES = 3
