# Development

> **This document owns:** how to run this repository on one machine — prerequisites, setup, the daily commands, and how to know it is working. *(skeleton, write me)*

<!-- concern: develop; fill: packages, tree, ci, env, services -->

## Prerequisites

*Runtimes and tools with the versions the repo pins or its CI installs. Name the file that pins each one.*

## Setup

*From clone to first successful run, as numbered commands. Include copying example env files by name; never a value.*

## Daily commands

*The handful of commands people actually type: run, test, lint, build. Copy the script or target names from the manifest or Makefile; do not paraphrase them.*

## How to know it works

*The URL, port, health route, or test command that proves the setup is alive.*

## Common problems

*Dated bullets. Each one names the symptom and the fix, with the file that was wrong.*
