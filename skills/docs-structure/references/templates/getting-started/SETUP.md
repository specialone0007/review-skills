# Setup

> **This document owns:** the path from a clean machine to a first visible success - prerequisites, install, run, what success looks like, and what to do when it fails. *(skeleton, write me)*

*Read this if you have never run this repository.*

<!-- concern: setup; fill: packages, tree, ci, env, services -->

## Prerequisites

*Runtimes and tools with the versions the repo pins or its CI installs. Name the file that pins each one.*

## Install

*From clone to installed, as numbered commands: the install command is a checked copy of AGENTS.md § Commands (the one place a command is repeated), plus copying example env files by name; never a value.*

## Run it

*The one command that starts it - a checked copy of AGENTS.md § Commands - and the URL, port or prompt that appears. From the manifest, never paraphrased.*

## First success

*The smallest thing a new person can do to see it work: a request, a page, a command and its output.*

## If it fails

*Dated bullets. Symptom, cause, fix, the file that was wrong.*
