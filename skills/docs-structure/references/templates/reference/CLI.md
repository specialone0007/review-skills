# CLI reference

> **This document owns:** the commands this repository exposes — each command, its arguments and flags, and what it does. *(skeleton, write me)*

*Read this if you run it from a shell.*

<!-- concern: commands; fill: cli, packages, readme -->

## Install and invoke

*How the binary or script is installed and what the command is called. From the manifest, not the README.*

## Commands

*One subsection per command: what it does, arguments, flags with defaults. Copy names from the parser definitions.*

## Exit codes and output

*What non-zero means, and the shape of stdout when it is machine-readable.*

## Configuration

*Config files and environment variables the CLI reads. Names only.*
