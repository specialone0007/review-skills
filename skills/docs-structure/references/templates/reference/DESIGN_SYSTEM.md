# Design system

> **This document owns:** the visual system — tokens, components, and the rules for adding UI without redesigning. *(skeleton, write me)*

*Read this if you are building a screen.*

<!-- concern: design; fill: frontend, packages, tree -->

> **Golden rule:** the existing design IS the design system. New UI composes what exists; it does not introduce a font, a palette or a component library.

## Foundations

*Colours, type, spacing, radius, motion — as the tokens actually named in the code, with the file that defines them.*

## Components — reuse, do not rebuild

*The primitives that exist and where they live. A new screen is a composition of these.*

## Patterns

*Page layouts, empty states, loading states, error states. One example each, pointing at a real screen.*

## Do and do not

*Short pairs. Each "do not" names the mistake it was written after.*

## Pre-flight checklist

*What to check before merging any UI change: tokens only, both themes, mobile width, keyboard focus, no new dependency.*
