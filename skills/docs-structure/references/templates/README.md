# <project>

*One sentence: what this is and who it is for. The index reuses this line.*

> **This document owns:** the front door - what this repository is, the quickstart to a first success, the hand-off to the docs, the layout of the tree, the commands as the manifests name them, and where the licence is. Facts a doc owns are one line and a link here. *(skeleton, write me)*

<!-- concern: readme; fill: readme, packages, tree, services, env, ci, tests -->

## What it is

*Three sentences at most: what it does, for whom, and the one idea it hangs on. The long form is PRODUCT's; link it.*

## Quickstart

*Numbered: prerequisites with pinned versions, install, run, the URL or command that proves it works. Every command copied verbatim from the manifest with its source in brackets. SETUP owns the full path; this is the sixty-second version.*

<!-- docs-structure: start here -->
<!-- /docs-structure: start here -->

## Repository layout

*A table, one row per top-level folder or unit: folder, what lives there, its own README when it has one. From the tree, never invented.*

## Commands

*A table: task, command, source - install, run, test, lint, build, deploy. The command as it is typed, the file it comes from. AGENTS.md links here and repeats none of it.*

## Configuration

*One line and a link to CONFIGURATION.md (or the example env file). Never an environment table here: names have one home.*

## Status

*Version from the manifest, the licence file by name, the CI workflow by name. One line each.*
