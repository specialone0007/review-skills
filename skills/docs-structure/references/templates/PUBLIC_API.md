# Public API

> **This document owns:** what importers of this library get — the exported surface, how to call it, and what is considered stable. *(skeleton, write me)*

<!-- concern: exports; fill: exports, packages, tests, decisions -->

## Install and import

*The package name, the registry, and the import line. From the manifest.*

## Exported surface

*Every public export from the entry file, grouped: functions, types, constants. Names from the entry file, not the folder.*

## Usage

*One real example per major export, taken from the examples folder or tests when they exist.*

## Stability and versioning

*What semver means here, what is experimental, what is deprecated and since when. Dates from tags.*
